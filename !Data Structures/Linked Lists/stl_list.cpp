#include <iostream>
#include <list>

using namespace std;

int main() {


  // Functions: 
  
  // l.push_back(value);    // Adds 'value' to the end of the list O(1)
  // l.push_front(value);   // Adds 'value' to the front of the list O(1)
  // l.pop_back();          // Removes the last element O(1)
  // l.pop_front();         // Removes the first element O(1)
  // l.clear();             // Clears all elements from the list O(n)
  // l.insert(iter, value); // Inserts 'value' before the element at the specified iterator position O(1)
  // l.erase(iter);         // Removes the element at the specified iterator position O(1)
  // l.begin();             // Returns an iterator to the first element O(1)
  // l.end();               // Returns an iterator to the past-the-last element O(1)
  // l.size();              // Returns the number of elements in the list O(n)
  // l.empty();             // Returns true if the list is empty O(1)
  // l.front();             // Returns the first element O(1)
  // l.back();              // Returns the last element O(1)
  // l.reverse();           // Reverses the order of the elements in the list O(n)
  // l.sort();              // Sorts the elements in the list O(n log n)
  // l.unique();            // Removes consecutive duplicates from the list O(n)
}
